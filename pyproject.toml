[project]
name = "rtms-documentation"
version = "0.1.0"
description = "Central documentation for the RTMS cybersecurity platform"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs>=1.6.0",
    "mkdocs-material>=9.5.0",
]

[tool.uv]
package = false
